/// <reference types="cypress" />

// eslint-disable-next-line no-undef
describe('Create a New Item', () => {
  // it kinda like a headless browser thing. So Javascript works
  // fine there
  console.log('Running cy after a very long time');
});
